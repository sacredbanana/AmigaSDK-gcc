#ifndef INLINE4_LAYERS_H
#define INLINE4_LAYERS_H

/*
** This file was auto generated by idltool 51.8.
**
** It provides compatibility to OS3 style library
** calls by substituting functions.
**
** Do not edit manually.
*/

#ifndef EXEC_TYPES_H
#include <exec/types.h>
#endif
#ifndef EXEC_EXEC_H
#include <exec/exec.h>
#endif
#ifndef EXEC_INTERFACES_H
#include <exec/interfaces.h>
#endif

#ifndef GRAPHICS_LAYERS_H
#include <graphics/layers.h>
#endif
#ifndef GRAPHICS_CLIP_H
#include <graphics/clip.h>
#endif
#ifndef GRAPHICS_RASTPORT_H
#include <graphics/rastport.h>
#endif
#ifndef GRAPHICS_REGIONS_H
#include <graphics/regions.h>
#endif

/* Inline macros for Interface "main" */
#define InitLayers(li) ILayers->InitLayers(li)
#define CreateUpfrontLayer(li, bm, x0, y0, x1, y1, flags, bm2) ILayers->CreateUpfrontLayer(li, bm, x0, y0, x1, y1, flags, bm2)
#define CreateBehindLayer(li, bm, x0, y0, x1, y1, flags, bm2) ILayers->CreateBehindLayer(li, bm, x0, y0, x1, y1, flags, bm2)
#define UpfrontLayer(dummy, layer) ILayers->UpfrontLayer(dummy, layer)
#define BehindLayer(dummy, layer) ILayers->BehindLayer(dummy, layer)
#define MoveLayer(dummy, layer, dx, dy) ILayers->MoveLayer(dummy, layer, dx, dy)
#define SizeLayer(dummy, layer, dx, dy) ILayers->SizeLayer(dummy, layer, dx, dy)
#define ScrollLayer(dummy, layer, dx, dy) ILayers->ScrollLayer(dummy, layer, dx, dy)
#define BeginUpdate(l) ILayers->BeginUpdate(l)
#define EndUpdate(layer, flag) ILayers->EndUpdate(layer, flag)
#define DeleteLayer(dummy, layer) ILayers->DeleteLayer(dummy, layer)
#define LockLayer(dummy, layer) ILayers->LockLayer(dummy, layer)
#define UnlockLayer(layer) ILayers->UnlockLayer(layer)
#define LockLayers(li) ILayers->LockLayers(li)
#define UnlockLayers(li) ILayers->UnlockLayers(li)
#define LockLayerInfo(li) ILayers->LockLayerInfo(li)
#define SwapBitsRastPortClipRect(rp, cr) ILayers->SwapBitsRastPortClipRect(rp, cr)
#define WhichLayer(li, x, y) ILayers->WhichLayer(li, x, y)
#define UnlockLayerInfo(li) ILayers->UnlockLayerInfo(li)
#define NewLayerInfo() ILayers->NewLayerInfo()
#define DisposeLayerInfo(li) ILayers->DisposeLayerInfo(li)
#define FattenLayerInfo(li) ILayers->FattenLayerInfo(li)
#define ThinLayerInfo(li) ILayers->ThinLayerInfo(li)
#define MoveLayerInFrontOf(layer_to_move, other_layer) ILayers->MoveLayerInFrontOf(layer_to_move, other_layer)
#define InstallClipRegion(layer, region) ILayers->InstallClipRegion(layer, region)
#define MoveSizeLayer(layer, dx, dy, dw, dh) ILayers->MoveSizeLayer(layer, dx, dy, dw, dh)
#define CreateUpfrontHookLayer(li, bm, x0, y0, x1, y1, flags, hook, bm2) ILayers->CreateUpfrontHookLayer(li, bm, x0, y0, x1, y1, flags, hook, bm2)
#define CreateBehindHookLayer(li, bm, x0, y0, x1, y1, flags, hook, bm2) ILayers->CreateBehindHookLayer(li, bm, x0, y0, x1, y1, flags, hook, bm2)
#define InstallLayerHook(layer, hook) ILayers->InstallLayerHook(layer, hook)
#define InstallLayerInfoHook(li, hook) ILayers->InstallLayerInfoHook(li, hook)
#define SortLayerCR(layer, dx, dy) ILayers->SortLayerCR(layer, dx, dy)
#define DoHookClipRects(hook, rport, rect) ILayers->DoHookClipRects(hook, rport, rect)
#define LayerOccluded(layer) ILayers->LayerOccluded(layer)
#define HideLayer(layer) ILayers->HideLayer(layer)
#define ShowLayer(layer, infront_of) ILayers->ShowLayer(layer, infront_of)
#define SetLayerInfoBounds(li, bounds) ILayers->SetLayerInfoBounds(li, bounds)
#define AllocClipRect(li) ILayers->AllocClipRect(li)
#define FreeClipRect(li, cr) ILayers->FreeClipRect(li, cr)
#define CreateLayerA(li, tlist) ILayers->CreateLayerA(li, tlist)
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define CreateLayer(...) ILayers->CreateLayer(__VA_ARGS__)
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define CreateLayer(vargs...) ILayers->CreateLayer(## vargs)
#endif
#define ChangeLayerShape(layer, region, hook) ILayers->ChangeLayerShape(layer, region, hook)
#define CreateBackFillHookA(tlist) ILayers->CreateBackFillHookA(tlist)
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define CreateBackFillHook(...) ILayers->CreateBackFillHook(__VA_ARGS__)
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define CreateBackFillHook(...) ILayers->CreateBackFillHook(## vargs)
#endif
#define DeleteBackFillHook(hook) ILayers->DeleteBackFillHook(hook)
#define SetBackFillHookAttrsA(h, tags) ILayers->SetBackFillHookAttrsA(h, tags)
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define SetBackFillHookAttrs(...) ILayers->SetBackFillHookAttrs(__VA_ARGS__)
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define SetBackFillHookAttrs(vargs...) ILayers->SetBackFillHookAttrs(## vargs)
#endif
#define GetBackFillHookAttrsA(h, tags) ILayers->GetBackFillHookAttrsA(h, tags)
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define GetBackFillHookAttrs(...) ILayers->GetBackFillHookAttrs(__VA_ARGS__)
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define GetBackFillHookAttrs(vargs...) ILayers->GetBackFillHookAttrs(## vargs)
#endif
#define ComposeLayersA(l, rect, taglist) ILayers->ComposeLayersA(l, rect, taglist)
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define ComposeLayers(l, ...) ILayers->ComposeLayers(l, __VA_ARGS__)
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define ComposeLayers(l, vargs...) ILayers->ComposeLayers(l, ## vargs)
#endif
#define GetOffScreenLayerBitMap(l, fallback, bounds) ILayers->GetOffScreenLayerBitMap(l, fallback, bounds)
#define SetLayerInfoAttrsA(li, taglist) ILayers->SetLayerInfoAttrsA(li, taglist)
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define SetLayerInfoAttrs(...) ILayers->SetLayerInfoAttrs(__VA_ARGS__)
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define SetLayerInfoAttrs(vargs...) ILayers->SetLayerInfoAttrs(## vargs)
#endif
#define GetLayerInfoAttrsA(li, taglist) ILayers->GetLayerInfoAttrsA(li, taglist)
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define GetLayerInfoAttrs(...) ILayers->GetLayerInfoAttrs(__VA_ARGS__)
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define GetLayerInfoAttrs(vargs...) ILayers->GetLayerInfoAttrs(## vargs)
#endif
#define ChangeLayerAlpha(layer, clips, hook) ILayers->ChangeLayerAlpha(layer, clips, hook)
#define SetForegroundAlpha(layer, templateptr, templatetype, bytesperrow, left, top, width, height, flags) ILayers->SetForegroundAlpha(layer, templateptr, templatetype, bytesperrow, left, top, width, height, flags)
#define SetLayerOpaqueness(layer, opaqueness) ILayers->SetLayerOpaqueness(layer, opaqueness)
#define SetLayerOverlayRect(layer, left, top, width, height, flags) ILayers->SetLayerOverlayRect(layer, left, top, width, height, flags)

#endif /* INLINE4_LAYERS_H */
