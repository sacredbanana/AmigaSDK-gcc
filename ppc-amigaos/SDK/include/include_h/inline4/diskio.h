#ifndef INLINE4_DISKIO_H
#define INLINE4_DISKIO_H

/*
** This file was auto generated by idltool 53.7.
**
** It provides compatibility to OS3 style library
** calls by substituting functions.
**
** Do not edit manually.
*/ 

#ifndef EXEC_TYPES_H
#include <exec/types.h>
#endif
#ifndef EXEC_EXEC_H
#include <exec/exec.h>
#endif
#ifndef EXEC_INTERFACES_H
#include <exec/interfaces.h>
#endif

#ifndef UTILITY_TAGITEM_H
#include <utility/tagitem.h>
#endif
#ifndef LIBRARIES_DISKIO_H
#include <libraries/diskio.h>
#endif
#include <interfaces/diskio.h>

/* Inline macros for Interface "main" */
#define Setup(name, tags) IDiskIO->Setup((name), (tags)) 
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define SetupTags(...) IDiskIO->SetupTags(__VA_ARGS__) 
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define SetupTags(vargs...) IDiskIO->SetupTags(## vargs) 
#endif
#define Cleanup(dio) IDiskIO->Cleanup((dio)) 
#define Update(dio) IDiskIO->Update((dio)) 
#define IsDiskPresent(dio) IDiskIO->IsDiskPresent((dio)) 
#define IsWriteProtected(dio) IDiskIO->IsWriteProtected((dio)) 
#define Query(dio, tags) IDiskIO->Query((dio), (tags)) 
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define QueryTags(...) IDiskIO->QueryTags(__VA_ARGS__) 
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define QueryTags(vargs...) IDiskIO->QueryTags(## vargs) 
#endif
#define FlushIOCache(dio) IDiskIO->FlushIOCache((dio)) 
#define FlushBlocks(dio, block, blocks) IDiskIO->FlushBlocks((dio), (block), (blocks)) 
#define ReadBlocks(dio, block, buffer, blocks) IDiskIO->ReadBlocks((dio), (block), (buffer), (blocks)) 
#define WriteBlocks(dio, block, buffer, blocks) IDiskIO->WriteBlocks((dio), (block), (buffer), (blocks)) 
#define ReadBytes(dio, offset, buffer, bytes) IDiskIO->ReadBytes((dio), (offset), (buffer), (bytes)) 
#define WriteBytes(dio, offset, buffer, bytes) IDiskIO->WriteBytes((dio), (offset), (buffer), (bytes)) 
#define AddDiskChangeHandler(name, func, udata, tags) IDiskIO->AddDiskChangeHandler((name), (func), (udata), (tags)) 
#if (defined(__STDC_VERSION__) && __STDC_VERSION__ >= 199901L) || (__GNUC__ >= 3)
#define AddDiskChangeHandlerTags(name, func, ...) IDiskIO->AddDiskChangeHandlerTags((name), (func), __VA_ARGS__) 
#elif (__GNUC__ == 2 && __GNUC_MINOR__ >= 95)
#define AddDiskChangeHandlerTags(name, func, vargs...) IDiskIO->AddDiskChangeHandlerTags(name, func, ## vargs) 
#endif
#define RemDiskChangeHandler(dch) IDiskIO->RemDiskChangeHandler((dch)) 

#endif /* INLINE4_DISKIO_H */
